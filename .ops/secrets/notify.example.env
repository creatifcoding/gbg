# Example SHAPE only — NOT a secret, NOT auto-loaded. Real values go into an
# encrypted notify.sops.env via `sops -e`. Mirrors ../mac-mini-morning-window/notify.env.template.
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=gbg-REPLACE-WITH-RANDOM
MAIL_TO=you@example.com
MAIL_FROM=
SMTP_URL=
SMTP_USER=
SMTP_PASS=
