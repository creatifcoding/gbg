# Field guide — gbg#24 (mantis-ee-02)

S1/S2/Q1 structural modes come from the interface registry.

Read `INDEX.md`, `contracts/interfaces.json`, and the generated task packet under `.agents/control/packets/24.json` before editing.
Stop on second identical failure, >15% write-set growth, missing source, or a new cross-domain decision.
