# Planned jobset (not executed)

KiCad 8 jobset planning file for the issue-26 fixture panel.

This file is **not** an executed CAM run. `kicad-cli` is absent. Fabricator
stackup is UNVERIFIED. Do not treat listed outputs as produced artifacts.

IPC-D-356, IPC-2581, and ODB++ jobs are omitted on purpose: the pinned
toolchain/fabricator path is not verified.
