[project]
name = "limitlessrp"
version = "0.0.1"
description = "Limitless Research Protocol Python utilities for commodity intelligence fixtures"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python/limitlessrp"]
