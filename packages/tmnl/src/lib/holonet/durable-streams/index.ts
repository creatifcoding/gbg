/**
 * Durable-Streams ↔ NATS Integration
 *
 * Effect-native wrapper for durable-streams protocol that fronts NATS
 * for last-mile clients (UIs, ERP, HMI, SCADA/MES terminals).
 *
 * @module holonet/durable-streams
 */

// ─── Services ────────────────────────────────────────────────────────────────
export * from './services';
