# Planned CAM outputs (not produced)

Gerber X2, Excellon, position, assembly/fabrication PDFs, netlist, STEP, and
raw ERC/DRC reports are **not** in this directory. Inventing empty Gerber
files would falsely imply a CAM run.

`export-status.json` and `manifest.sha256` are written by
`ee/kicad/fixture/tools/check_locks.py` from the native source digest.

IPC-D-356 / IPC-2581 / ODB++ remain unclaimed.
