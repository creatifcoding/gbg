# gotby

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gotby` to build the library.

## Running unit tests

Run `nx test gotby` to execute the unit tests via [Vitest](https://vitest.dev/).
