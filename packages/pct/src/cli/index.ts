/**
 * @tmnl/pct/cli — the `pact` CLI.
 *
 * Built on `effect/unstable/cli`. Composable command tree:
 *   pact ─┬── registry ── status
 *         └── publish
 *
 * @module @tmnl/pct/cli
 */

export { pact } from "./commands.js"
