# Field guide — gbg#23 (mantis-ee-01)

Source interfaces/KiCad libs; consume contracts/interfaces.json.

Read `INDEX.md`, `contracts/interfaces.json`, and the generated task packet under `.agents/control/packets/23.json` before editing.
Stop on second identical failure, >15% write-set growth, missing source, or a new cross-domain decision.
