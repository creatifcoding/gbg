# CAD packet

Review packet for this cut of `@tmnl/mantis-terrarium-jscad`. Class generated / DRAFT. Not shop-release.

Authoring and the generate recipe live in `README.md`. View axes live in `generated/VIEWS.md`. This file does not invent a second recipe.

## This tree has

Envelope cuboid 250 x 250 x 500 mm. Origin front-left-bottom. X right, Y back, Z up.

`npm test` on this coupon exited 0. Ten tests passed, zero failed. Test `envelope is one 250 x 250 x 500 mm cuboid at front-left-bottom origin` still proves bbox `[0,0,0]→[250,250,500]`.

`npm run typecheck` on this coupon exited 0.

`npm run generate` on this coupon exited 0 and wrote:

| file | view | axis | class |
| --- | --- | --- | --- |
| `generated/front.svg` | front | `[0, 1, 0]` | generated |
| `generated/side.svg` | side | `[1, 0, 0]` | generated |
| `generated/top.svg` | top | `[0, 0, 1]` | generated |

Those SVGs are class generated, not shop-release. Axes are the ones already recorded in `README.md` and `generated/VIEWS.md`.

## Named solids this cut authored

| solid | balloon | modeled | omitted |
| --- | --- | --- | --- |
| `B01-corner-block` | B01 | 24 mm REF cubes from `frame.band`; eight instances at envelope corners | leftover pocket/inset cuts |
| `B05-view-cassette` | B05 | 202 x 3 x 427 mm plate | world placement |
| `B06-front-door` | B06 | 202 x 3 x 427 mm plate | world placement |

Placement of B05 and B06 is UNVERIFIED. Cassette seat is `stock + 0.20 mm` TARGET (3.20 mm). Recorded, not a pocket.

## Draft in-house print

Path is FDM on Bambu. Printer model is UNVERIFIED. Nozzle and bed stay unnamed. Full note is in `README.md`.

Printed candidates are REF printed solids. Acrylic B05 and B06, screen B11, and bought Particle/ADI parts are not printed. Filament (PETG/ASA on B01) stays REF.

## Not in this packet

Leftover S00–S11 are leftover extract only. They are not Generated by this cut.

PR 34 and PR 36 leftover FreeCAD/OCCT stay emit. This cut did not copy leftover stills, STEP, or OCCT into this tree.

`emitStl` and `emitStep` stay not implemented. No cuboid shop drawing. No MPNs. No prices. Merge stays held.
