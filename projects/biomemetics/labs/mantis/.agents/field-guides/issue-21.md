# Field guide — gbg#21 (mantis-00a-runtime)

Owns flake/Nix/CI; disjoint from #22 write set.

Read `INDEX.md`, `contracts/interfaces.json`, and the generated task packet under `.agents/control/packets/21.json` before editing.
Stop on second identical failure, >15% write-set growth, missing source, or a new cross-domain decision.
