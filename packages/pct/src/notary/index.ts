/**
 * @tmnl/pct/notary — the automagic authoring surface.
 *
 * Witness, validate, timestamp, record. The Notary handles the
 * registry-event metadata callers shouldn't have to populate manually.
 *
 * @module @tmnl/pct/notary
 */

export {
  type DeprecateOptions,
  type NotaryShape,
  type RegisterSchemaOptions,
  Notary,
} from "./Notary.js"
export { Default } from "./Default.js"
