# Draft shop index pointer

This is **not** a reviewed EvidenceRecord and **not** SHOP-RELEASE.

The DRAFT shop catalog (STEP/SVG/PNG/PDF + SHA-256 + honesty class) is:

`projects/biomemetics/labs/mantis/terrarium/shop/`

Lifecycle is `generated`. It does not rewrite `terrarium/MANIFEST.sha256`.
Do not order or energize from it.
