/**
 * Genifer Testbed — temporarily disabled for production build.
 * The testbed imports symbols not yet exported from genifer/react (WIP refactor).
 * Re-enable when genifer barrel exports are complete.
 */
export default function GeniferTestbed() {
  return (
    <div className="flex items-center justify-center h-full text-zinc-500">
      <p>Genifer Testbed — temporarily disabled (WIP refactor)</p>
    </div>
  )
}
