# A4a terrarium card

Paints a simulated plant. Open the gallery with `npm run dev` after `npm install`.

The card reads `PlantView` props. It does not call a gateway. Missing CopilotKit `runtimeUrl` is not required to paint known, stale, simulated, faulted, and unavailable.
