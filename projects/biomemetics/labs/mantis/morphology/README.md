# Morphology

Structure descriptions and measured geometry derived from observations,
photogrammetry, microscopy, literature, or supplied CAD. Every item records its
basis as `observed`, `measured`, `calculated`, `simulated`, `ref`, `target`,
`typ`, or `unverified`.

Taxonomic interpretation remains optional and explicitly qualified.
