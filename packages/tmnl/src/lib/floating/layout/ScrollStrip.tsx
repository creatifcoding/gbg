/**
 * ScrollStrip — backward-compatible re-export shim.
 *
 * Implementation decomposed into `layout/scroll-strip/`.
 * Keep this module path stable for existing consumers.
 *
 * @module floating/layout/ScrollStrip
 */

export { ScrollStrip, type ScrollStripProps } from './scroll-strip'
export { default } from './scroll-strip'
