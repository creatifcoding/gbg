# Simulations

Shared biological and engineered simulation studies. Each run records inputs,
solver/tool version, mesh or timestep configuration, outputs, and the claim it
can and cannot support.

Simulation results are evidence about a model. They are not physical test
results and must not upgrade a `TARGET` to `VERIFIED` without the corresponding
bench evidence.

