# Field guide — gbg#31 (mantis-06-release)

Shop release only after all gates; immutable evidence package.

Read `INDEX.md`, `contracts/interfaces.json`, and the generated task packet under `.agents/control/packets/31.json` before editing.
Stop on second identical failure, >15% write-set growth, missing source, or a new cross-domain decision.
