{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SensorTransitionEvent": {
      "type": "object",
      "required": [
        "sensor_id",
        "from",
        "to",
        "at"
      ],
      "properties": {
        "sensor_id": {
          "$ref": "#/definitions/SensorId"
        },
        "from": {
          "$ref": "#/definitions/SensorStatus"
        },
        "to": {
          "$ref": "#/definitions/SensorStatus"
        },
        "action": {
          "anyOf": [
            {
              "$ref": "#/definitions/SensorTransitionAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "at": {
          "type": "string",
          "description": "ISO8601 UTC or offset timestamp string.",
          "title": "ISO8601 Timestamp",
          "format": "date-time"
        },
        "reason": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "initiated_by": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "description": "Transition payload contract for sensor lifecycle events.",
      "title": "Sensor Transition Event"
    },
    "SensorId": {
      "type": "string",
      "description": "Sensor identifier in SNS-{slug} format.",
      "title": "Sensor ID",
      "pattern": "^SNS-[a-zA-Z0-9-]+$"
    },
    "SensorStatus": {
      "type": "string",
      "enum": [
        "active",
        "calibrating",
        "needs_calibration",
        "faulted",
        "offline",
        "decommissioned"
      ],
      "description": "ISA-95 control-module lifecycle status for sensor assets.",
      "title": "Sensor Status"
    },
    "SensorTransitionAction": {
      "type": "string",
      "enum": [
        "StartCalibration",
        "CompleteCalibration",
        "FailCalibration",
        "FlagForCalibration",
        "MarkFaulted",
        "ClearFault",
        "TakeOffline",
        "BringOnline",
        "Decommission"
      ],
      "description": "Named transition actions from canonical sensor lifecycle graph.",
      "title": "Sensor Transition Action"
    }
  },
  "type": "object",
  "required": [
    "sensor_id",
    "from",
    "to",
    "at"
  ],
  "properties": {
    "sensor_id": {
      "$ref": "#/definitions/SensorId"
    },
    "from": {
      "$ref": "#/definitions/SensorStatus"
    },
    "to": {
      "$ref": "#/definitions/SensorStatus"
    },
    "action": {
      "anyOf": [
        {
          "$ref": "#/definitions/SensorTransitionAction"
        },
        {
          "type": "null"
        }
      ]
    },
    "at": {
      "type": "string",
      "description": "ISO8601 UTC or offset timestamp string.",
      "title": "ISO8601 Timestamp",
      "format": "date-time"
    },
    "reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "initiated_by": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "description": "Generated from Effect Schema (src/lib/maidens/domains/contracts/sensor/ts/sensor.contract.ts). Canonical transition event contract for TypeScript and Elixir.",
  "title": "SensorTransitionEvent",
  "$id": "/contracts/sensor/sensor_transition.schema.json"
}
