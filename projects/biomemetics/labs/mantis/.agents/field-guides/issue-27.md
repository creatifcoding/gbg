# Field guide — gbg#27 (mantis-ee-05)

Measured evidence under evidence/runs/...; theoretical fixtures are not measured.

Read `INDEX.md`, `contracts/interfaces.json`, and the generated task packet under `.agents/control/packets/27.json` before editing.
Stop on second identical failure, >15% write-set growth, missing source, or a new cross-domain decision.
